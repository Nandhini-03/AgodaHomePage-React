import React from 'react'

function Footer2Links(props) {
    // console.log(props.name)
  return (
    <li>
        <a href="#" className='footerLinks'>{props.name}</a>
    </li>
  )
}

export default Footer2Links
